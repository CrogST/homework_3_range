#pragma once

#include <string>
#include <vector>

//using vi = std::vector<int>;
//template<typename T>
//using v = std::vector<T>;
using ip = std::vector<std::string>;
using ip_list = std::vector<ip>;

extern bool ip_cmp(ip a, ip b);
